
Simply you need to:

    git clone <repo>
    cp config/smtp_gmail.yml.example config/smtp_gmail.yml # and edit
    rake db:setup

And you should be ready to go:

    script/server


# Obsolete

#as sudoer launch `bin/install-prerequisites`

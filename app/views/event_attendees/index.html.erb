<h1>Listing event_attendees</h1>

<table>
  <tr>
    <th>Calendar</th>
    <th>Person</th>
    <th>Confirmed</th>
    <th>Notes</th>
  </tr>

<% @event_attendees.each do |event_attendee| %>
  <tr>
    <td><%=h event_attendee.calendar_id %></td>
    <td><%=h event_attendee.person_id %></td>
    <td><%=h event_attendee.confirmed %></td>
    <td><%=h event_attendee.notes %></td>
    <td><%= link_to 'Show', event_attendee %></td>
    <td><%= link_to 'Edit', edit_event_attendee_path(event_attendee) %></td>
    <td><%= link_to 'Destroy', event_attendee, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New event_attendee', new_event_attendee_path %>